import { Routes } from '@angular/router';
import { ToolListComponent } from './pages/tool-list/tool-list/tool-list.component';

export const routes: Routes = [
    { path: '', component: ToolListComponent }
];
